@model Sale.Models.OrderSearchArg
@{
    ViewBag.Title = "Index";
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <h1>查詢訂單</h1>
    @*@<div>@Html.TextBox("textorderID")</div>
        @Html.DropDownList("custData", (List<SelectListItem>)ViewBag.custData);*@
    @using (Html.BeginForm("Index", "Order", FormMethod.Post, new { id = "Form1" }))
    {
        <div id="SearchConditionSection">
            <table border="1">
                <tr>
                    <td>訂單編號</td>
                    <td colspan="4">@Html.TextBoxFor(model => model.OrderId)</td>
                </tr>
                <tr>
                    <td>客戶名稱</td>
                    <td colspan="4">@Html.TextBoxFor(model => model.CustName)</td>
                </tr>
                <tr>
                    <td>負責員工</td>
                    <td colspan="4">@Html.DropDownListFor(model => model.EmpId, (List<SelectListItem>)ViewBag.EmpCodeData, "")</td>
                </tr>
                <tr>
                    <td>出貨公司</td>
                    <td colspan="4">
                    @Html.DropDownListFor(model => model.ShipperId, (List<SelectListItem>)ViewBag.ShipCodeData, "")</td>
                </tr>
                <tr>
                    <td>訂購日期</td>
                    <td><input type="date" id="bookdate"></td>
                    <td>出貨日期</td>
                    <td><input type="date" id="deliverydate"></td>
                </tr>
                <tr>
                    <td>需要日期</td>
                    <td colspan="4"><input type="date" id="demanddate"></td>
                </tr>
                <tr>
                    <td></td>
                    <td colspan="4">
                        <input type="submit" value="查詢" />
                        <input type="button" value="刪除" />
                        <input type="button" id="add" value="新增訂單" />
                    </td>
                </tr>
            </table>
        </div>

        <div id="Search">


            @if (ViewBag.SearchResult != null)
            {
                <table class="table table-bordered table-striped">
                    <tr>
                        <td>訂單編號</td>
                        <td>客戶名稱</td>
                        <td>訂購日期</td>
                        <td>發貨日期</td>
                        <td></td>
                        <td></td>
                    </tr>
                    @foreach (var item in (List<Sale.Models.Order>)ViewBag.SearchResult)
                    {
                        <tr id='@item.OrderId'>
                            <td>@item.OrderId</td>
                            <td>@item.CustName</td>
                            <td>@item.Orderdate</td>
                            <td>@item.ShippedDate</td>
                            <td>@Html.ActionLink("修改", "Update", new { id = item.OrderId })</td>
                            <td>
                                <a class="btnDelete" href="#">刪除</a>
                                <input class="hidOrderId" type="hidden" value='@item.OrderId' />
                            </td>
                        </tr>
                    }
                </table>
            }
        </div>
    }
</body>
</html>
